// Program to print the first 10 even numbers using a for loop

#include <stdio.h>

int main() {
    printf("First 10 even numbers:\n");
    for (int i = 0; i <= 18; i += 2) {
        printf("%d\n", i);
    }
    return 0;
}
